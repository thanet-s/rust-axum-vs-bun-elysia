# Compiled binaries
/target/
**/*.rs.bk

# These are backup files generated by the `cargo fmt` command
**/*.rs.bk

# Files generated by the `cargo doc` command
/doc/

# Environment variables file (used by tools like `dotenv`)
.env

# If using rustup to manage toolchains, you might want to ignore toolchain files
rust-toolchain
rust-toolchain.toml

# If using Cargo's workspaces feature, you might want to ignore the lock file at the root
# (however, it's typically recommended to check-in your lock files)
#Cargo.lock
